FROM node:22-alpine
 
# Set working directory
WORKDIR /app
 
# Install bash for build scripts
RUN apk add --no-cache bash
 
# Configure npm cache and permissions
RUN mkdir -p /.npm && chmod -R 777 /.npm
ENV NPM_CONFIG_CACHE=/tmp/.npm
 
# Copy source code
COPY . .